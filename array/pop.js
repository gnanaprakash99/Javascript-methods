let numbers=[1,2,3,4];
numbers.pop();
numbers.pop();  // removing the last element of an array and then return the last element

console.log(numbers); //result: [1,2] 
console.log(numbers.pop()); //result:2 (array length)